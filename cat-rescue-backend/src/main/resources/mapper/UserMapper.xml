<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.catrescue.catrescueplatform.repository.UserRepository">
    
    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultType="com.catrescue.catrescueplatform.entity.User">
        SELECT * FROM users WHERE username = #{username}
    </select>
    
    <!-- 根据邮箱查询用户 -->
    <select id="findByEmail" resultType="com.catrescue.catrescueplatform.entity.User">
        SELECT * FROM users WHERE email = #{email}
    </select>
    
</mapper>